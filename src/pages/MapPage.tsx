// ============================================================
// AMAN CEMENT CRM — Full Screen Map Page
// ============================================================

import { MapContainer } from '@/components/map';

export function MapPage() {
  return (
    <div className="h-[calc(100vh-4rem)]">
      <MapContainer />
    </div>
  );
}
